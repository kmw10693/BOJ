#include <bits/stdc++.h>

using namespace std;

int t;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> t;
    
    while(t--) {
        int n, m;
        cin >> n >> m;
        
        while(m--) {
            int a, b;
            cin >> a >> b;
        }
        cout << (n-1) << '\n';
    }

}